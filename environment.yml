name: geo_env
channels:
  - patrikhlobil
  - conda-forge
  - defaults
dependencies:
  - geopandas=0.6.2=py_0
  - pip:
    - keplergl==0.1.2
    - topojson==1.0rc7
    - traittypes==0.2.1
    - pandas
    - numpy
    - seaborn
    - matplotlib
prefix: /usr/local/anaconda3/envs/geo_env

